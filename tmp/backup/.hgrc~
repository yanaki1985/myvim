require_relative "word_from_string"
require_relative "count_frequency"

raw_text =%{ The problem breaks down into two parts. First, given some text as a string, return a list of words. That sounds like an array. Then, build a count for each distinct word. That sounds like a user for a hash --- we can index it with the word and use the corresponding entry to keep a count}

for i in 0..5
  
end
